#include "binary_trees.h"

/**
 * binary_tree_is_perfect - function which checks if a binary -
 * - tree is perfect or not
 *   @tree: pointer to the root node
 *   Return: 0 or i
 */

int binary_tree_is_perfect(const binary_tree_t *tree)
{
	int count = 0;

}
